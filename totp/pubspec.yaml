name: another_authenticator_totp
description: TOTP library used by another_authenticator.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=2.18.6 <3.0.0'

dependencies:
  uuid: 3.0.4
  crypto: ^3.0.1

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
