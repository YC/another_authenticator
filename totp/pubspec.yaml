name: another_authenticator_totp
description: TOTP library used by another_authenticator.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  uuid: 3.0.7
  crypto: ^3.0.3

dev_dependencies:
  lints: ^2.1.1
  test: ^1.24.3
